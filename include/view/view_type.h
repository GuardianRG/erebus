#pragma once

namespace erebus {

/**
 * This enum holds every type of view that can be added dynamically to the gui.
 */
enum class ViewType {
	/**
	 * An empty view.
	 *
	 * Only used for testing purpose.
	 */
	EMPTY_VIEW
};
}//namespace erebus